@inherits LayoutComponentBase

<div class="dashboard-layout">
    <aside class="sidebar">
        <!-- Logo Section -->
        <div class="logo-container">
            <img src="images/logo.png" 
                 alt="Water Station Logo" 
                 class="logo-image" />
            <h1 class="system-name">Water Station Online Appointment</h1>
        </div>
        
        <h2 style="text-align: center;">Admin</h2>

        <nav>
            <a href="/admin/dashboard" class="nav-link @(IsActive("/admin/dashboard") ? "active" : "")">
                <i class="fas fa-tachometer-alt"></i> Dashboard
            </a>
            <a href="/admin/home" class="nav-link @(IsActive("/admin/home") ? "active" : "")">
                <i class="fas fa-home"></i> Home
            </a>
            <a href="/admin/orders" class="nav-link @(IsActive("/admin/orders") ? "active" : "")">
                <i class="fas fa-box"></i> Orders
            </a>
            <a href="/admin/feedback" class="nav-link @(IsActive("/admin/feedback") ? "active" : "")">
                <i class="fas fa-star"></i> Feedback
            </a>
            <a href="/admin/about" class="nav-link @(IsActive("/admin/about") ? "active" : "")">
                <i class="fas fa-info-circle"></i> About Us
            </a>
        </nav>
        
        <!-- Account and Logout buttons positioned at bottom -->
        <div class="bottom-container">
            <a href="/admin/account" class="nav-link @(IsActive("/admin/account") ? "active" : "")">
                <i class="fas fa-user-circle"></i> Account
            </a>
            <a href="/login" class="nav-link logout-link">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </div>
    </aside>

    <main class="main-content">
        <div class="background-overlay"></div>
        <div class="page-container">
            @Body
        </div>
    </main>
</div>

@code {
    private bool IsActive(string href)
    {
        var uri = NavigationManager.Uri.Replace(NavigationManager.BaseUri, "/");
        return uri.StartsWith(href, StringComparison.OrdinalIgnoreCase);
    }

    [Inject] private NavigationManager NavigationManager { get; set; } = default!;
}


<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

